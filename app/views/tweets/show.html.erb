<h1>Geektwitter</h1>
<h3>レビューの詳細</h3>
<div class="tweet">
  <p><%= @tweet.body %></p>
  <p><%= @tweet.created_at %></p>
  <p>
  <%= raw sanitize_with_hashtags(@tweet.body) %>
  </p>
</div>
<% @tweet.tags.each do |tag| %>
    <span><%= tag.name %></span>
<% end %>
<%= link_to "編集する", edit_tweet_path(@tweet.id) %>

<%= link_to "レビュー投稿一覧に戻る", tweets_path %>
